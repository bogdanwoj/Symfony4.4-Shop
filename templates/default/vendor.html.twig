{% extends 'base.html.twig' %}

{% block title %}Vendors{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-12"><h2> {{ vendor.name }} </h2>  </div>

            {% for product in vendor.getProducts() %}
                {{ include ('default/product/productCard.html.twig') }}
            {% endfor %}

        </div>
    </div>
{% endblock %}
