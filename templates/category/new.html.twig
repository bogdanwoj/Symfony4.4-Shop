{% block body %}
    {{ include('category/form.html.twig') }}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <a href="{{ path('category_index') }}">Back</a>
            </div>
        </div>
    </div>
{% endblock %}