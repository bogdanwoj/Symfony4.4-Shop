{% extends 'base.html.twig' %}

{% block title %}vendor{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12"><h2>Vendor {{ vendor.name }}</h2></div>
            <div class="col-1 btn btn-success"><a href="{{ path('vendor_edit', {'id': vendor.id}) }}" class="text-light">Edit</a></div>
            <div class="col-3">{{ include('vendor/delete.html.twig') }}</div>
            <div class="col-12">
                <table class="table">
                    <tbody>
                    <tr>
                        <th>Id vendor</th>
                        <td>{{ vendor.id }}</td>
                    </tr>
                    <tr>
                        <th>Nume vendor</th>
                        <td>{{ vendor.name }}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <a href="{{ path('vendor_index') }}">Back</a>
    </div>


{% endblock %}