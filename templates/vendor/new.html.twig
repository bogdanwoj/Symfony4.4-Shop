{% block body %}
    {{ include('vendor/form.html.twig') }}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <a href="{{ path('vendor_index') }}">Back</a>
            </div>
        </div>
    </div>
{% endblock %}