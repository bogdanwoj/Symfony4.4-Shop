{% block body %}
    {{ include('crud_product/_form.html.twig') }}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <a href="{{ path('crud_product_index') }}">Back</a>
            </div>
        </div>
    </div>
{% endblock %}