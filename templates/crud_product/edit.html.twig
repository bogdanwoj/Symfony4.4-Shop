{% block body %}
    <div class="container">
        <div class="row mt-5">

            <div class="col-12 ">
                <a href="{{ path('crud_product_index') }}">back to list</a>
                {{ include('crud_product/_form.html.twig', {'button_label': 'Update'}) }}
                <div class="col-12">{{ include('crud_product/_delete_form.html.twig') }}</div>
            </div>
        </div>
    </div>

{% endblock %}

