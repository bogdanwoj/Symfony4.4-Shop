{% block body %}
    <form method="post" action="{{ path('crud_product_delete', {'id': product.id}) }}" onsubmit="return confirm('Confirma stergerea!!!');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
        <button class="btn btn-danger">Delete</button>
    </form>
{% endblock %}

